#!/usr/bin/env python

#this code will take positions in right ascension and declination and use them 
#to model an orbit.

import ephem
import numpy as np
from numpy.linalg import eig, inv 
import matplotlib 
import matplotlib.pyplot as plt

#ra=some array
#dec=some other array such that len(dec)=len(ra)
#ra_error=an array of errors in ra
#dec_error=an array of errors in dec

plt.plot(ra,dec,'o')
plt.xlim([0,24]) #ra limits, zero to 24 hours
plt.ylim([-90,90]) #dec limits, -90 degrees to 90 degrees
plt.show()
